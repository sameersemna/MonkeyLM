[tool.ruff]
line-length = 110
target-version = "py311"
exclude = [
    "playwright_user_data",
    "reports",
    ".kilo",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle
    "F",   # Pyflakes
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "I",   # isort
]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
exclude = [
    "playwright_user_data",
    "reports",
    ".kilo",
]
